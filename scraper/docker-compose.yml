version: '3.8'

services:
  scraper:
    build: .
    container_name: uc-scraper
    volumes:
      - ../data:/data
    environment:
      - TZ=America/Los_Angeles
    command: ["-data=/data", "-workers=5", "-delay=1.0"]